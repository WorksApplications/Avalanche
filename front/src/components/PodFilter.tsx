import { Component, h } from "preact";
// @ts-ignore
import styles from "./PodFilter.scss";

class PodFilter extends Component {
  public render() {
    return <div className={styles.wrap}>{/* TODO Filter */}</div>;
  }
}

export default PodFilter;
